/**
 * Mission Control | NRJ Morgen
 * Dashboard for managing radio show agenda, stats, and news search
 *
 * NOTE: This file is a legacy reference stub. All application logic
 * has been moved to the Vite + React SPA (see src/).
 *
 * API keys and secrets MUST be provided through environment variables
 * (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_BRAVE_API_KEY).
 * See .env.example for the required variables.
 * NEVER commit credentials or API keys to source control.
 */

// All functionality has been moved to the React SPA in src/.
// See src/services/ for the current implementation.

console.log('🎛️ Mission Control loaded. See src/ for main application code.');
